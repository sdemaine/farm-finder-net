/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // distDir: 'dist',
    // Add any other Next.js config options you need
  };
  
  export default nextConfig;